#1732096084021 | took 3ms | statement | connection 0| url jdbc:p6spy:postgresql://localhost:5432/bookingapp

    create table corporations (
        deleted BOOLEAN DEFAULT FALSE,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        primary key (id)
    )

    create table corporations (
        deleted BOOLEAN DEFAULT FALSE,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        primary key (id)
    );

#1732098654776 | took 3ms | statement | connection 0| url jdbc:p6spy:postgresql://localhost:5432/bookingapp

    create table corporations (
        deleted BOOLEAN DEFAULT FALSE,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        primary key (id)
    )

    create table corporations (
        deleted BOOLEAN DEFAULT FALSE,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        primary key (id)
    );

#1732096084046 | took 4ms | statement | connection 0| url jdbc:p6spy:postgresql://localhost:5432/bookingapp

    create table subdivisions (
        deleted BOOLEAN DEFAULT FALSE,
        corporation_id bigint,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        adress varchar(255) not null,
        contact_info varchar(255),
        primary key (id)
    )

    create table subdivisions (
        deleted BOOLEAN DEFAULT FALSE,
        corporation_id bigint,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        adress varchar(255) not null,
        contact_info varchar(255),
        primary key (id)
    );

#1732098654806 | took 6ms | statement | connection 0| url jdbc:p6spy:postgresql://localhost:5432/bookingapp

    create table subdivisions (
        deleted BOOLEAN DEFAULT FALSE,
        corporation_id bigint,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        adress varchar(255) not null,
        contact_info varchar(255),
        primary key (id)
    )

    create table subdivisions (
        deleted BOOLEAN DEFAULT FALSE,
        corporation_id bigint,
        created_at timestamp(6),
        id bigint generated by default as identity,
        last_update_at timestamp(6),
        name varchar(100) not null,
        adress varchar(255) not null,
        contact_info varchar(255),
        primary key (id)
    );

------------------------------------------------------------------------------------------

@JsonBackReference
    @ManyToOne
    @JoinColumn(name = "time_card_id", nullable = false)
    private TimeCard timeCard;

@JsonManagedReference
    @OneToMany(mappedBy = "timeCard", orphanRemoval = true, fetch = FetchType.EAGER, cascade = CascadeType.ALL)
    private List<Activity> activities;